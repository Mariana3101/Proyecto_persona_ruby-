
<p id="notice"><%= notice %></p>

<%= link_to 'Nueva Persona', new_persona_path %>
<div class="card">
  <div class="card-body">
    <h1>Personas</h1>

    <h3>Promedio de edad en años</h3>
    <%= @promedioEdades %>
  </div>
</div>

<div class="table-responsive">

  <table class="table">
    <thead>
      <tr>
        <th scope="col">id</th>
        <th scope="col">nombre</th>
        <th scope="col">apellido</th>
        <th scope="col">nacimiento</th>
        <th scope="col">dni</th>
        <th scope="col"> </th>
        <th scope="col"> </th>
        <th scope="col"> </th>

      </tr>
    </thead>

    <tbody>
    <% @personas.each do |persona| %>

      <tr>
        <th scope="row"><%= persona.id %></th>
        <td ><%= persona.nombre %></td>
        <td ><%= persona.apellido %></td>
        <td ><%= persona.fecha_de_nacimiento %></td>
        <td ><%= persona.dni %></td>
        <td><%= link_to 'Detalle', persona %></td>
        <td><%= link_to 'Editar', edit_persona_path(persona) %></td>
        <td><%= link_to 'Borrar', persona, method: :delete, data: { confirm: 'Estas seguro?' } %></td>
   
      </tr>

    <% end %>
    </tbody>
  </table>

</div>
<br>
<br>

